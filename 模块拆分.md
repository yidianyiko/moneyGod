# MoneyGod · 软件模块拆分(分而治之)

> 硬件(财神手办)由另一组负责,本文档只覆盖软件。
> 原则:每个模块有明确的**输入/输出契约**,可对着 mock 独立开发,最后按契约拼装。

---

## 0. 数据流总览

```
用户/平台
  │
  ▼
[M1] A2A Gateway ──(A2A)── PandaAI 平台调用
  │  └──(REST/BFF)── [M5] Web 前端(灵光)
  ▼
[M2] 核心引擎 (Planner/Research/Strategy, DeepSeek V4 pro)
  │        │
  │        ▼
  │   [M3] 数据适配层 (PandaAI Skills: 行情/财务/因子/回测)
  ▼
[M4] 投顾表达 & 签文服务 (量化结论 → 签文 + 大白话 + 风险提示)
  │
  ▼
[合规校验] 贯穿 M4/M1,输出前强制校验
```

一次"每日抽签"的调用链:
`UI 请求 → Gateway → 核心引擎(Planner 调度 → 数据适配取数 → Research/Strategy 分析)→ 投顾表达生成签文 → 合规校验 → 返回 UI`

---

## 1. 模块清单与归属

| 模块 | 名称 | 主要赛道 | 一句话职责 | 建议 owner |
|---|---|---|---|---|
| **M1** | A2A Gateway 服务 | PandaAI 18 | 暴露 Agent Card,处理 A2A 协议 + 鉴权,同时给 UI 提供 REST 入口 | 后端/协议 |
| **M2** | Multi-Agent 核心引擎 | PandaAI 18 | Planner 编排 + Research/Strategy 分析,接 DeepSeek | 核心/AI |
| **M3** | 数据适配层 | PandaAI 18 | 封装 PandaAI 数据/回测 Skills,给引擎干净的数据 API | 数据 |
| **M4** | 投顾表达 & 签文服务 | 度小满 15 | 把量化结论翻译成签文 + 大白话建议 + 风险提示 | 产品/表达 |
| **M5** | Web 前端(灵光) | 度小满 15 / 灵光 03 | 抽签交互、资产看板、建议展示 | 前端 |
| **共享** | shared 契约包 | — | 所有模块共用的类型/Schema + 合规校验库 | 全员共建 |

---

## 2. 共享契约(shared 包)——最先定,谁都不能改

所有模块围绕这几个数据结构对齐。建议先把这个包定下来,再各自开工。

```ts
// 用户画像
type UserProfile = {
  userId: string;
  riskLevel: 'conservative' | 'balanced' | 'aggressive';
  ageStage?: string;          // 如 "职场新人"
  monthlyBudget?: number;     // 可投月结余
  goals?: string[];
};

// 单条持仓
type Holding = {
  assetType: 'stock' | 'fund' | 'cash';
  code: string;
  name: string;
  amount: number;   // 持有金额
  cost?: number;    // 成本
};

// 量化结论(M2/M3 产出,M4 消费)——内部结构,不直接给用户
type QuantConclusion = {
  date: string;
  marketSummary: string;        // 市场概览(专业口径)
  signals: Array<{
    target: string;             // 标的/资产
    action: 'buy' | 'hold' | 'reduce' | 'watch';
    reason: string;             // 依据(因子/数据)
    confidence: number;         // 0-1
  }>;
  dataSources: string[];        // 用了哪些数据,供合规溯源
};

// 最终对用户的输出(M4 产出,M5 展示)
type AdviceResult = {
  date: string;
  signature: string;            // 签文(交互外壳,一句话仪式感)
  plainAdvice: string;          // 大白话建议
  actionHint: '建议做' | '建议观望' | '注意风险';
  riskWarnings: string[];       // 必填,不能为空
  disclaimer: string;           // 必填:非玄学 + 投资有风险 + AI 边界
  detail?: QuantConclusion;     // 可选,给想看细节的用户
};
```

### 合规校验库(shared 内)
一个纯函数 `assertCompliant(r: AdviceResult): void`,输出前强制调用:
- `riskWarnings` 非空
- `disclaimer` 含"非玄学/投资有风险/AI 不构成保证"要素
- 命中禁用措辞(如"稳赚""必涨""保本高收益")直接抛错拦截

---

## 3. 各模块契约详解

### M1 · A2A Gateway 服务
- **对外**:
  - `GET /.well-known/agent-card.json` — 返回 Agent Card(PandaAI 评审访问)
  - A2A 协议任务端点(接收平台自然语言任务)
  - `POST /api/daily-draw` — 给 M5 前端用的 REST 入口
- **对内依赖**:调用 M2 `handleTask()` / `dailyDraw()`
- **职责**:协议适配、鉴权、超时控制(≤20 分钟)、把 M2 结果包成 A2A / REST 响应
- **可独立开发**:先用假的 M2(返回固定 AdviceResult)把协议和 Agent Card 跑通

### M2 · Multi-Agent 核心引擎
- **对外接口**:
  - `handleTask(task): Promise<AgentResult>` — A2A 通用任务入口(投研/策略问答)
  - `dailyDraw(profile: UserProfile, holdings: Holding[]): Promise<QuantConclusion>` — 每日抽签的分析
- **对内依赖**:M3 数据 API;DeepSeek V4 pro
- **内部 Agent**:Planner(拆解调度)/ Research(归因)/ Strategy(建议与风险)
- **可独立开发**:M3 用 mock 数据;先跑通 Planner→Strategy 产出 QuantConclusion

### M3 · 数据适配层
- **对外接口(给 M2)**:
  - `getQuote(code): Quote`
  - `getFundamentals(code): Fundamentals`
  - `getFactor(params): FactorData`
  - `runBacktest(strategy): BacktestResult`
  - `getTradingCalendar(): Calendar`
- **对内依赖**:PandaAI 数据/投研 Skills(拿到清单后接入)
- **可独立开发**:先返回 mock/静态样本数据,契约不变,后期换真接口
- **范围**:MVP 只做 A股 + 基金

### M4 · 投顾表达 & 签文服务
- **对外接口**:
  - `toAdvice(conclusion: QuantConclusion, profile: UserProfile): AdviceResult`
- **职责**:
  - 把专业结论翻译成小白能懂的**大白话**
  - 生成当日**签文**(仪式外壳)
  - 注入**风险提示 + 免责声明**,调用 `assertCompliant`
- **可独立开发**:输入用手写的 QuantConclusion 样例,产出 AdviceResult
- **这是度小满评分的关键表达层**,决定"小白看不看得懂""可信度"

### M5 · Web 前端(灵光)
- **消费接口**:`POST /api/daily-draw`(来自 M1),渲染 `AdviceResult`
- **页面**:抽签交互(动画)、资产看板、建议卡片、风险提示区、免责声明
- **可独立开发**:对着 `AdviceResult` 的 mock JSON 先把 UI 做出来
- **注意**:灵光只做 UI 层,不承载后端逻辑

---

## 4. 并行开发的解耦策略

1. **Day 0**:全员先敲定 `shared` 契约包(第 2 节的类型 + 合规库),冻结接口。
2. 之后每个模块**对着 mock 独立开发**:
   - M1 mock M2 → 先跑通 Agent Card / A2A / REST
   - M2 mock M3 → 先跑通多 Agent 产出 QuantConclusion
   - M3 mock 真数据 → 先给静态样本
   - M4 用手写 QuantConclusion → 先跑通签文+大白话+合规
   - M5 用 mock AdviceResult → 先做 UI
3. **联调顺序**:M3→M2(真数据接引擎)→ M4(真结论接表达)→ M1(串起来)→ M5(接真 API)。

---

## 5. 建议的仓库结构(monorepo)

```
moneyGod/
├── packages/
│   ├── shared/          # M2/M3/M4 共用:类型 + 合规校验库
│   ├── data-adapter/    # M3
│   ├── core-engine/     # M2
│   ├── advisor/         # M4
│   └── a2a-gateway/     # M1(部署为可访问服务,提供 Agent Card)
└── web-ui/              # M5(灵光,或独立前端工程)
```

---

## 6. 交付对齐

| 模块 | 交付即满足 |
|---|---|
| M1 + M2 + M3 | PandaAI:可访问 Agent Card、A2A 可调用、DeepSeek 底座、多 Agent、≤20min |
| M4 + M5 | 度小满:大白话投顾、风险提示、小白可用、产品闭环 |
| M4 合规库 | 度小满:可信度 / AI 边界 |
| M5(灵光搭) | 灵光 03:用灵光完成软件 |
