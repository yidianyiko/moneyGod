# 财神 MoneyGod · PandaAI「Next AI Trader」赛道提交说明文档

> 玄学皮 · 量化芯。一支协作式 AI 投研团队，把"因子研究 → 策略生成 → 回测验证 → 风险评估 → 报告撰写"
> 的完整投研流程，做成一个可被 A2A 协议调用的 Remote Agent。

---

## 1. Agent 基本信息

| 项 | 内容 |
|---|---|
| Agent 名称 | 财神 MoneyGod |
| 一句话简介 | 多 Agent 量化投研团队：Planner/Factor/Strategy/Backtest/Risk/Report 协作完成投研任务，核心是 Backtest→Strategy 的真实回测反馈闭环——策略不达标会被真实回测数据打回重新生成。所有绩效数字由确定性引擎计算，LLM 仅负责参数设计与解释；每条输出均含风险提示，不做任何收益承诺。 |
| 团队 | AdventureX 2026 · MoneyGod Team（_团队成员名单待补充_） |
| 赛道 | PandaAI 18 · Next AI Trader（同时覆盖度小满 Money Whisperer 15） |
| 版本 | 0.2.0 |

---

## 2. 部署信息 / 访问方式

| 项 | 内容 |
|---|---|
| Agent Card URL | `http://47.98.99.199:8000/.well-known/agent-card.json` |
| 服务根地址 | `http://47.98.99.199:8000/` |
| Web 演示页 | `http://47.98.99.199:8000/`（可视化交互，输入需求即可看到多 Agent 协作过程） |
| A2A 协议端点 | `POST http://47.98.99.199:8000/`（JSON-RPC，`message/send` / `tasks/get` 等） |
| REST 调试端点 | `POST http://47.98.99.199:8000/api/analyze`，body：`{"text": "...", "riskLevel": "conservative|balanced|aggressive"}` |
| 健康检查 | `GET http://47.98.99.199:8000/healthz` |
| 鉴权方式 | 当前评审阶段无需鉴权（公开可调用）；服务端对上游数据源（`panda_data`）使用账号密码鉴权，对 LLM（火山引擎 Ark）使用 API Key 鉴权，均通过环境变量注入，不在代码中出现明文 |
| 底座模型 | DeepSeek-Pro（经火山引擎 Ark 接入点调用），轻量规划任务使用低延时 Seed 模型 |
| 响应时间约束 | 单次多 Agent 工作流（含最多 3 轮回测反馈迭代）实测 30 秒 ～ 2 分钟，远低于赛道要求的 20 分钟上限 |
| 代码仓库 | https://github.com/yidianyiko/moneyGod（公开） |

---

## 3. 架构设计

### 3.1 分层架构

```
Web UI（financial-god 主题的抽签式交互页面，内嵌于服务中）
        │  POST /api/analyze
A2A Gateway（Starlette + a2a-sdk）
        │  agent-card.json ｜ JSON-RPC ｜ REST
Multi-Agent 核心引擎（DeepSeek-Pro / Seed 底座）
   Planner → Factor → Strategy → Backtest ⇄ Strategy（反馈闭环）→ Risk → Report
        │
数据适配层（panda_data：行情 / 财务 / 因子）
        │
合规护栏（贯穿输出前的最后一道校验）
```

### 3.2 Multi-Agent 协作流程（核心差异化设计）

```
用户任务（自然语言）
  │
  ▼
Planner    拆解意图 → 从文本中识别股票/基金代码，确定候选池
  │
  ▼
Factor     多因子横截面打分（动量/估值/质量/成长/低波，z-score 标准化，数字全部代码计算）
  │
  ▼
Strategy   据因子结果生成策略参数（因子权重/持仓数/调仓周期/回看窗口）——LLM 只产出参数，不产出任何绩效数字
  │
  ▼
Backtest   用真实历史行情自算滚动回测（无未来函数），产出年化收益/夏普/最大回撤/胜率  ┐
  │                                                                              │ 夏普未达标
  │        ◄─────────────────────── 携带真实回测数据打回 Strategy 重新生成参数 ───┘（最多 3 轮）
  ▼
Risk       组合风险画像：波动率/最大回撤/持仓集中度（HHI）+ 合规红线检测
  │
  ▼
Report     DeepSeek 把以上全部确定性数据汇总为可解释的中文投研报告（不得新增/篡改任何数字）
  │
  ▼
合规护栏   禁用措辞扫描替换 + 强制附加风险提示与免责声明
  │
  ▼
QuantConclusion + 完整协作轨迹（trace）→ 返回给 A2A / Web UI
```

**差异化亮点**：Backtest → Strategy 的反馈闭环是"真协作"而非简单串联——策略参数是否合格，由确定性回测引擎裁决，不合格则携带真实数字打回重新设计，最多迭代 3 轮。三个示例任务的实测 trace 中都可以看到这一闭环真实触发。

### 3.3 核心设计原则："数字由代码算，LLM 只做参数设计与解释"

- 所有绩效/统计数字（收益率、夏普、最大回撤、因子分、波动率、HHI 集中度……）均由 `quant/` 模块的纯函数确定性计算，可复现、可审计。
- LLM（Planner/Strategy/Report）只负责任务规划、策略参数设计、归因解释与报告撰写，被 prompt 显式约束"绝不编造收益/夏普等数字"。
- 这一原则既满足度小满对"可信度"的评分要求，也是本项目应对 PandaAI 平台未提供现成回测 Skill 这一约束的解决方案：既然平台没给，我们自己写一套可复现的回测引擎，反而更贴合"过程与结果清晰可解释"的赛道要求。

### 3.4 合规护栏

`compliance.py` 是纯确定性模块（不调用 LLM），贯穿所有对外输出：
- **禁用措辞扫描**：命中"稳赚/必涨/保本/包赚/翻倍/零风险"等词汇时自动替换为中性表达。
- **强制免责声明**：确保每条输出都携带完整的风险提示文案（非投资建议、不构成收益承诺、AI 不代客理财等要素），已通过全部三个示例任务实测验证。

---

## 4. 数据 Skills / 投研 Skills 使用清单

| 能力 | 平台是否提供 | 本项目用法 |
|---|---|---|
| 价量因子（open/high/low/close/volume/amount/turnover/market_cap/net_profit/revenue） | ✅ `panda_data.get_factor` | Factor Agent 直接取用，计算动量、波动 |
| 基本面数据（ROE/EPS/毛利/净利同比/BVPS 等 43 字段） | ✅ `panda_data.get_fina_performance` | Factor Agent 计算估值代理（PE=市值/净利）与成长质量因子 |
| 行情数据（A股 `get_market_data`、场内基金 `get_fund_daily`） | ✅ | 数据适配层获取标的与大盘走势，供归因分析与展示 |
| 行业归属（`get_stock_industry`） | ✅ | 标的分析中展示行业标签 |
| 交易日历（`get_last_trade_date`） | ✅ | 确定回测/取数的截止交易日 |
| 策略回测 | ❌ 平台未提供回测 Skill | 自研确定性回测引擎（`quant/backtest.py`）：滚动窗口横截面打分 → 选 top_k 等权持有 → 真实收盘价结算 → 输出年化/夏普/回撤/胜率/卡玛，无未来函数 |
| 组合风险分析 | ⚠️ 无现成 Skill | 自算：年化波动率、最大回撤、HHI 持仓集中度、风险等级判定（`quant/risk.py`） |

---

## 5. 示例任务与预期输出（3 个，均为对公网部署地址的真实调用）

> 以下均为 `2026-07-24` 对 `http://47.98.99.199:8000/api/analyze` 的真实 HTTP 调用结果，非人工编造。

### 示例任务 1：多标的多因子策略构建 + 回测（反馈闭环完整触发）

**输入**
```json
{"text": "用 600519.SH 000858.SZ 601318.SH 构建稳健的多因子策略并回测", "riskLevel": "balanced"}
```

**协作轨迹（13 步，含 3 轮策略-回测反馈迭代）**
```
Planner  解析任务意图,确定候选池 → [600519.SH, 000858.SZ, 601318.SH]
Factor   多因子打分(动量/估值/质量/成长/低波),横截面 z-score
Strategy 生成初始策略参数
Backtest 第 1 轮真实回测（数字由引擎计算）
Planner  回测未达标(夏普 -5.509 < 0.5)，打回 Strategy 重生成
Strategy 第 2 轮据反馈调整参数
Backtest 第 2 轮真实回测
Planner  回测未达标(夏普 -4.238 < 0.5)，打回 Strategy 重生成
Strategy 第 3 轮据反馈调整参数
Backtest 第 3 轮真实回测
Backtest 买入持有基准对照
Risk     组合风险画像(波动/回撤/集中度) + 合规红线
Report   汇总为可解释投研报告(含风险提示)
```

**因子排名**：`600519.SH`(0.3522) > `601318.SH`(0.0643) > `000858.SZ`(-0.4166)

**最终策略参数**：因子权重 `{momentum:0.1, low_vol:0.6, value:0.15, quality:0.15}` · 持仓 2 只 · 调仓 10 日 · 回看 20 日

**回测结果**（自算，可复现）：年化 -51.04% ｜ 夏普 -3.927 ｜ 最大回撤 -26.99% ｜ 胜率 42.2% ｜ 90 个交易日、7 次调仓
基准（等权买入持有）：年化 -25.09% ｜ 夏普 -1.259 ｜ 最大回撤 -27.27%
是否跑赢基准：否（如实呈现，未粉饰结果）

**组合风险**：波动率 21.19% ｜ 最大回撤 -27.27% ｜ 持仓集中度 HHI 0.333 ｜ 风险等级 中

**投研报告节选**：
> 策略在90天回测期内未跑赢买入持有基准，其年化收益率为-0.5104，低于基准年化收益率-0.2509；风险调整后表现亦弱于基准，策略夏普比率-3.927，基准夏普比率-1.259……需注意回测区间仅90个交易日，参数对特定市场环境的适应性存在不确定性，因子表现可能出现阶段性失效。

**免责声明**（附加在输出末尾）：
> 【风险提示】以上为基于历史行情数据的量化分析,非投资建议,不构成任何收益承诺。市场有风险,投资需谨慎;历史表现不代表未来收益。请结合自身风险承受能力独立决策。本服务由 AI 生成,不代客理财,数据可能存在延迟或缺失。

---

### 示例任务 2：双标的因子分析与风险评估（保守型风险偏好）

**输入**
```json
{"text": "分析 600036.SH 和 000333.SZ 的因子表现与风险", "riskLevel": "conservative"}
```

**因子排名**：`600036.SH`(0.0) ≈ `000333.SZ`(-0.0)（区间内因子差异极小）

**最终策略参数**：因子权重 `{momentum:0.25, low_vol:0.35, value:0.15, quality:0.2, growth:0.05}` · 持仓 2 只 · 调仓 20 日 · 回看 60 日（经 3 轮迭代，趋向更长周期以降低交易噪声，符合"稳健型"用户设定）

**回测结果**：年化 -21.24% ｜ 夏普 -1.633 ｜ 最大回撤 -7.92% ｜ 胜率 42.5% ｜ 40 个交易日
基准：年化 +25.03% ｜ 夏普 1.574 ｜ 最大回撤 -7.92%
是否跑赢基准：否

**组合风险**：波动率 14.89% ｜ 最大回撤 -7.92% ｜ HHI 0.5 ｜ 风险等级 低

**投研报告节选**：
> 策略在回测期内年化回报为-21.24%，显著落后于买入持有基准的25.03%……回测天数仅40个交易日，结果的稳定性存在较大不确定性，不构成对未来表现的任何保证。

免责声明与示例 1 相同，已确认存在于输出末尾。

---

### 示例任务 3：市场问答（无标的代码，走真实大盘数据锚定的 QA 路径）

**输入**
```json
{"text": "今天A股大盘怎么样?市场情绪偏乐观还是谨慎", "riskLevel": "balanced"}
```

**回答节选**（基于当日真实上证/沪深300/创业板指数据）：
> 上证指数收报 3876.78 点，涨 +0.25%；沪深300收报 4728.00 点，涨 +0.23%；创业板指收报 3575.52 点，涨 +0.25%……90日动量：上证指数 -5.13%，沪深300 -0.90%，创业板指 -2.01%……今日情绪偏暖，但整体仍偏谨慎：今日普涨且跌幅较小，市场情绪较此前明显修复，但从区间位置和90日动量看，市场仍处于"大跌后的低位整理"状态，并未形成趋势性反转信号……所有分析仅基于历史数据客观描述，不构成任何投资建议，不对未来收益做任何承诺。

数据来源：`panda_data:0.0.12`，免责声明同样附加在输出末尾。

---

## 6. 已知限制与后续计划

- 20 分钟响应时间目前只是配置常量（`MAX_RESPONSE_SECONDS`），尚未包装成硬性超时熔断；实测响应远低于该上限，风险可控但计划补上。
- 尚未接入 QuantSkills 官方投研 Skills（回测/绩效分析部分已用自研引擎替代，因平台未提供对应 Skill）。
- A2A 通用任务入口的结果缓存（同输入不重复调用，节省额度）尚未实现。
- 财神手办硬件（涂鸦 T5 带屏 + 舵机）为 P1 加分项，暂未开发。
- 演示视频待录制。
